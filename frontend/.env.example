# No API URL settings needed: the app always calls /api and /sanctum on its
# own origin. Locally, vite.config.js proxies them to http://localhost:8000;
# on Vercel, vercel.json rewrites them to the Render backend.
